import { pageMetadata } from "@/lib/metadata";
export const metadata = pageMetadata({ title: "Poachland — Your gear", description: "Trade jerseys and collect discs with the ultimate community.", noIndex: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
